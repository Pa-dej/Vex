[package]
name = "hello_plugin"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[dependencies]
bytes = "1.10"
tokio = { version = "1", features = ["rt"] }
vex-proxy-sdk = { path = "../../vex-sdk", features = ["metrics"] }
